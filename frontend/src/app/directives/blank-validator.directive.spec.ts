import { BlankValidatorDirective } from './blank-validator.directive';

describe('BlankValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new BlankValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
